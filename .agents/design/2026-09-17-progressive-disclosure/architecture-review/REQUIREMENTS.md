# Observable requirements

- An agent can start at the root README, choose an app or package, then read that area's README to identify ownership and public entry points.
- The root map includes every workspace category, including the existing checkout service.
- Architecture checks detect an app importing another app, a package importing app code, or an admin feature importing app composition internals.
